<section class="section kontakt-section" id="kontakt">
  <div class="container">
    <span class="section-label">Kontakt</span>
    <h2>Porozmawiajmy o Dolinie Agatów</h2>

    <div class="contact-grid">
      <div class="contact-copy prose-justify">
        <p>
          Masz pytanie dotyczące działalności stowarzyszenia, lokalnego dziedzictwa albo
          wydarzeń w Płóczkach Górnych i Nagórzu? Aktualne kanały kontaktu internetowego
          opublikujemy tutaj, gdy tylko będą gotowe.
        </p>
        <p class="contact-note">
          Do tego czasu można skontaktować się z nami osobiście lub korespondencyjnie pod
          adresem siedziby.
        </p>
      </div>

      <address class="contact-card">
        <span class="contact-card-label">Siedziba stowarzyszenia</span>
        <strong>Dolina Agatów</strong>
        <span>Płóczki Górne 4A</span>
        <span>59-600 Lwówek Śląski</span>
      </address>
    </div>
  </div>
</section>

<style>
  .kontakt-section {
    background: var(--heban);
    color: rgb(250 246 239 / 0.82);
  }

  .kontakt-section h2 { color: var(--krem); }

  .contact-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.3fr) minmax(15rem, 0.9fr);
    gap: clamp(1.5rem, 3vw, 2.5rem);
    align-items: start;
    max-width: 54rem;
  }

  .contact-copy { font-size: 1.03rem; }
  .contact-copy p { margin-block: 0 1rem; }
  .contact-note { color: rgb(250 246 239 / 0.62); }

  .contact-card {
    display: flex;
    flex-direction: column;
    padding: 1.4rem 0 0 1.35rem;
    border: 0;
    border-left: 2px solid var(--bursztyn);
    font-style: normal;
  }

  .contact-card-label {
    margin-bottom: 0.75rem;
    color: var(--bursztyn-jasny);
    font-size: 0.75rem;
    font-weight: 650;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .contact-card strong {
    margin-bottom: 0.3rem;
    color: var(--krem);
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 570;
  }

  @media (max-width: 720px) {
    .contact-grid { grid-template-columns: 1fr; gap: 1.5rem; }
  }
</style>
